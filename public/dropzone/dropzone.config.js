// var md = new Dropzone(".dropzone", {
//     maxFilesize: "5", 
//     addRemoveLinks: true 
// });
// var md = new Dropzone(".dropzone");
// md.on("complete", function (file) {
//     if (this.getUploadingFiles().length === 0 && this.getQueuedFiles().length === 0) {
//         $('#list-files').load(location.href + ' #list-files','');
//     }

//     // md.removeFile(file); 
// });